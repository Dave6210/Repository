# Repository
人机协作报告

第一部分：AI 交互日志
在本项目开发过程中，我与 AI 进行了深度协作。起初，我向 AI 描述了作业的四个模块需求（M1 数据处理、M2 分析可视化、M3 预测模型、M4 问答接口）。协作初期，AI 帮助我快速搭建了代码的整体骨架，实现了基本的 Parquet 文件读取和简单的图表绘制。
随着项目的深入，交互变得更加具象化。当遇到全量数据处理（300万条记录）导致的内存溢出（MemoryError）时，我向 AI 寻求优化方案，AI 建议采用“分层抽样”技术并提供了改进的代码段。在集成测试阶段，出现了多个模块间的接口调用错误（ImportError）和数据类型不匹配问题，我通过截取报错信息并反馈给 AI，双方共同排查，最终通过统一函数命名规范、加强异常值（NaN 和 Inf）处理以及修正列名大小写敏感（如 vendorid 修正为 VendorID）等手段，确保了系统的稳定性。

第二部分：代码生成的三个阶段及对比

第一阶段：基础实现期。
该阶段的代码主要关注功能的“从无到有”。实现了基础的 Pandas 读取逻辑和单张折线图生成。
对比分析：此阶段代码较为脆弱，未考虑异常值和大数据量导致的性能问题，仅能处理小规模样本，且各模块独立运行，缺乏系统集成。

第二阶段：功能补全与健壮性提升期。
在这一阶段，我们补齐了 M2 的四项核心分析任务，并在 M3 中引入了 PyTorch 神经网络。为了应对报错，增加了对“除零异常”导致的无穷大数值的过滤，并引入了 2 个衍生特征（高峰时段判断、每英里费率）。
对比分析：代码开始具备工业级处理能力，增加了数据质量报告输出。通过将无穷大数值替换为缺失值并剔除，解决了模型训练时的崩溃问题，但仍存在少许命名不一致的细节错误。

第三阶段：系统优化与最终交付期。
这是目前的最终版本。代码实现了 M1 到 M4 的一键式流水线运行。M3 增加了 Loss 曲线绘制和随机森林对比逻辑，M1 优化了输出格式以便于阅读。
对比分析：此阶段代码达到了作业的高分要求。预测指标 MAE 稳定在 8.06 左右，RMSE 在 11.57 左右，逻辑严密，且问答系统能准确识别“JFK 机场（ID 132）”和“18点高峰”等关键业务结论。

第三部分：反思
通过本次人机协作，我深刻认识到 AI 在编程中是极佳的“辅助驾驶员”，而非替代者。在处理 M3 的神经网络构建时，AI 能迅速给出架构模板，极大地节省了查阅文档的时间。然而，在面对具体数据的“脏逻辑”（如纽约出租车数据中的零距离行程）时，AI 无法预知所有边界情况，必须由人类通过报错分析来进行逻辑修正。
我也意识到，良好的代码规范和接口定义是大型项目成功的基石。早期因为函数命名不统一导致的反复调试让我学到了：在编写多模块程序前，先商定好 API 规范能减少 50% 以上的无效工作。最后，对于 300 万条规模的数据，如何在性能和准确性间取得平衡（如本项目中采用的 10 万条代表性采样策略）是数据科学实践中极具价值的经验。

Tips：项目代码和分析报告已完整上传。因原始 Parquet 数据集超过 GitHub 存储限制（453MB），故未包含在仓库中，运行代码前请确保本地 data 文件夹下已有 2023年1月的黄色出租车数据。
